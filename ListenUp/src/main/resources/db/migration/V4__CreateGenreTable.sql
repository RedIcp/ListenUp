CREATE TABLE `genre` (
                         `id` int NOT NULL,
                         `name` varchar(45) NOT NULL,
                         PRIMARY KEY (id),
                         UNIQUE (name)
);
