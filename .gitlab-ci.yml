stages:
- build
- test 

build:
  stage: build
  script:
    - cd ListenUp
    - ./gradlew build -x test
    - ./gradlew.bat --stop 
test:
  stage: test
  script:
    - cd ListenUp
    - ./gradlew test
    - ./gradlew.bat --stop
